# SANDBOX-glazed — Session Log
# =========================================================
# Running notes for AI assistant continuity between sessions.
# Update this at the end of each session before closing.
# =========================================================


## PROJECT OVERVIEW

A Drupal 11 Lando site that embeds micro-frontend apps (React, Astro) as
Drupal blocks via a custom CLI toolkit at 3pd-ide/starter-scripts/cli/.

CLI command: `3pd`
Key commands:
  3pd astro app <name>          Create new Astro app
  3pd astro module --install    Build + install as Drupal block module
  3pd react app <name>          Create new React app
  3pd react module --install    Build + install as Drupal block module


## ARCHITECTURE

Lando services (2 only — do NOT add more):
  appserver   → Drupal 11 at http://sandbox-glazed.lndo.site
  database    → MariaDB 10.6

IMPORTANT: The astro Lando service was REMOVED. It caused HSTS poisoning of
all *.lndo.site subdomains. Do NOT add it back. No framework gets a Lando service.

React apps: Express runs on the HOST machine via npm run dev. No Lando service.
Astro apps: No Express. No Lando service. Static build + Drupal JSON:API.


## STARTER KITS

3pd-ide/apps/0.starter-react/        → React + Vite + Express + SQLite ✓ DONE
3pd-ide/apps/0.starter-astro/        → Astro SSG + Drupal JSON:API (read-only) ✓ DONE
3pd-ide/apps/0.starter-astro-forms/  → Astro SSG + Express + SQLite ✓ DONE

EACH STARTER HAS A PROMPT.txt — READ IT AT THE START OF EVERY SESSION.
The PROMPT.txt files are the single source of truth for how each starter works.
They document architecture decisions, known issues, and lessons learned.
Always read the relevant PROMPT.txt before touching any starter kit code.

  0.starter-astro/PROMPT.txt       — Astro read-only pipeline, HAProxy gotchas, JSON:API patterns
  0.starter-astro-forms/PROMPT.txt — Astro + Express + SQLite, forms pattern, prod deployment
  0.starter-react/PROMPT.txt       — React pipeline, known issues pending fix (cleanup next)

3PD REPO STRATEGY (decided 2026-03-27):
Each starter kit will live in its own standalone repo. The 3PD (third-party) dev
clones only their relevant starter repo — they never see /web or the Drupal codebase.
`3pd <framework> module` (no --install) packages the Drupal module subfolder.
The 3PD dev pushes a feature branch. The Drupal owner pulls it and runs --install.
See "3PD WORKFLOW" section below for full details.


## ACTIVE APPS

react---3pd-depot           → Drupal module: hudx_react___3pd_depot
react---3pd-poc             → Drupal module: hudx_react___3pd_poc
react---stracat             → Drupal module: hudx_react___stracat
react---3pd-ai-helper       → Drupal module: hudx_react___3pd_ai_helper (AI code gen + Drupal page create) ✓ LIVE
react---3pd-embed-request   → Drupal module: hudx_react___3pd_embed_request (Smart Embed/Stand-Alone Link form)
astro---poc-01              → Drupal module: hudx_astro___poc_01 (static, working)
astro---poc-02              → Drupal module: hudx_astro___poc_02
astro-forms---3pd-checklist → Drupal module: hudx_astro_forms___3pd_checklist ✓ LIVE


## LANDO COMMANDS

lando crx   — Full cache reset. Does: rm files/php/ + TRUNCATE all cache tables
              + stop Apache + drush status (pre-warms container into DB) + restart Apache.
              Use after every lando rebuild AND after enabling/installing modules.

lando drush — Standard drush. DO NOT use for cache clearing. Use lando crx.


## CRITICAL: lando crx / cache reset procedure

After lando rebuild OR after enabling modules OR when drush hangs:
  1. lando crx        ← handles everything
  2. Open browser     ← site loads immediately

NEVER use `lando drush cr` — it hangs. See SANDBOX-glazed/.lando.yml for
the full crx command. Root cause: Apache workers deadlock compiling the Drupal
service container simultaneously when cache is empty. crx stops Apache first,
pre-warms via drush status, then restarts Apache.

If URLs don't resolve after lando restart:
  lando poweroff && lando start   (re-registers proxy routes)
  If still broken: lando rebuild -y, then immediately lando crx


## SESSION: 2026-03-26 — What we fixed / built

### Astro starter kit — fully rebuilt
- Removed: Express, Prisma, SQLite, start.sh, Lando service entirely
- New architecture: Astro SSG + client-side Drupal JSON:API fetch
- No separate backend. No Lando service. Data from Drupal's own JSON:API.
- starter-template now has: package.json (astro only), index.astro (JSON:API
  client fetch), .env.example (PUBLIC_DRUPAL_BASE_URL for dev)
- astro-app.js: removed Prisma setup, removed start.sh patching, skips
  node_modules during copy (fixes broken .bin symlinks)
- react-app.js: same node_modules skip fix applied
- create-module.js: changed `lando drush cr` → `lando crx`
- PROMPT.txt written for 0.starter-astro (full reference doc)

### JSON:API module — enabled
- Was not enabled on this Drupal site
- Could not enable via `lando drush en` because of missing `canvas` module code
  (config exists in DB but no .info.yml on disk)
- Workaround: enabled via `drush php:eval` using module_installer service
- Routes activated via `drupal_flush_all_caches()` via php:eval

### astro---poc-02 — created and deployed
- Created with `3pd astro app poc-02`
- Updated index.astro to use `basic_page_layout_builder` (this site has no
  `page` content type nodes — types are: drag_and_drop_page,
  basic_page_layout_builder)
- Deployed with `3pd astro module --install`
- Block renders in Drupal but shows "No pages found."


## SESSION: 2026-03-27 — What we fixed / built

### astro---poc-02 — WORKING. Shows live Drupal data.

Root causes found and fixed:

1. HAProxy strips square brackets from query strings.
   `?fields[node--basic_page_layout_builder]=title` → `?fields=` (empty)
   Fix: removed the sparse fieldset param entirely. Plain URL with no params works.
   Note: %5B/%5D encoding also gets stripped by HAProxy — do NOT use brackets
   in query strings for Lando-proxied fetches.

2. Browser cache served stale JS after rebuild.
   `?v=1.x` was hardcoded in libraries.yml — never changed between builds.
   Fix: 0.starter-astro/create-module.js now uses Date.now() as version.
   Every rebuild produces a new ?v=<timestamp> cache buster.

3. Old starter template default URL was node/page — this site has no `page` nodes.
   Fix: poc-02 index.astro updated to fetch basic_page_layout_builder.
   The starter template (0.starter-astro) still uses node/page as placeholder —
   this is correct since it's generic. Apps must update it for their content type.

Block at node/6 now displays:
  - SANDBOX - 3PD Depot
  - SANDBOX - Stracat
  - Sandbox - Layout Builder


## SESSION: 2026-03-27 — Strategy decisions (no code changes)

### 3PD repo strategy — decided
Each starter kit gets its own standalone git repo.
3PD devs clone only their framework's repo. No /web, no Drupal, no Lando required.
`3pd <framework> module` (without --install) packages the module subfolder only.
3PD dev pushes a feature branch. Drupal owner pulls + runs --install.
The --install flag is INTERNAL USE ONLY (copies to /web + enables in Drupal).

### Data persistence — decided
Apps that collect data (forms etc.) use Express + SQLite. NOT Drupal's DB.
Reasons:
  1. Portability: if client changes CMS, Express + SQLite file moves intact.
  2. Compliance: .gov/HUD site — data must stay within FedRAMP boundary.
     No external PaaS (Railway, Render etc.) for data storage on this project.
  3. Simplicity: SQLite is a file, zero infrastructure overhead vs PostgreSQL.

In production: Express backend co-deployed on the Drupal server, behind the
same reverse proxy / auth wall as Drupal. Module JS calls /api/app-name/ on
the same domain — already protected by the auth layer, no extra token needed.

SQLite file location:
  Dev:  ./server/db/app.sqlite  (relative, inside app folder)
  Prod: /var/app-data/<appname>/app.sqlite  (absolute, outside web root)

### Client context — HUD (.gov federal agency)
The Drupal site is behind a sign-in wall for a HUD federal agency.
Federal guidance applies (FedRAMP, FISMA, NIST 800-53, Section 508).
Apps ONLY exist as Drupal modules in production — never standalone.
All data infrastructure must stay within the compliance boundary.
No unauthorized cloud services. Data must be US-hosted on approved infra.

### Starter kit plan — agreed order
1. Finish 0.starter-astro (read-only SSG — nearly done)
2. Build 0.starter-astro-forms (Astro SSG + Express + SQLite)
3. Update 0.starter-react (apply all lessons: SQLite, version string, drush path, cors)
4. Further frameworks (Vanilla JS, Angular, Svelte) follow the same pattern

### Two Astro starter variants
Read-only apps (display CMS content only):
  0.starter-astro → Astro SSG, no backend, no DB. Current pattern.
  Use when: app only needs to show Drupal content.

Apps with forms / data persistence:
  0.starter-astro-forms → Astro SSG + Express + SQLite.
  Express listens on 127.0.0.1 only (not 0.0.0.0 — not a public service).
  Use when: app needs to collect or store data.

### Mandatory checklist for ALL module generators (create-module.js)
See 0.starter-astro/PROMPT.txt for the full list. Key items:
  - version in libraries.yml = Date.now() (never static 1.x)
  - No square brackets in fetch URLs (HAProxy strips them)
  - lando crx for cache clear (never lando drush cr)
  - node_modules skipped during template copy
  - Express listens on 127.0.0.1 only in production builds
  - DB_PATH configurable via env var
  - Use .env.production for production build values (Vite/Astro reads this
    automatically during `astro build`). Set both PUBLIC_DRUPAL_BASE_URL=
    and PUBLIC_API_BASE_URL= to blank. .env keeps dev values (full URLs).
    This pattern is now in all starter templates — no manual blanking needed. (not hardcoded)


## SESSION: 2026-03-27 — Context compacted, picking up canvas fix

Canvas module remnants were blocking `lando drush en`. Investigation complete.
Canvas fix was IN PROGRESS when context ran out. See "WHERE WE LEFT OFF" below.

Root cause confirmed:
- canvas/canvas_stark appear in active Drupal config (core.extension) in the DB
- canvas appears in system.schema key_value table in the DB
- canvas-related sync config YAML files may exist in sites/default/files/config_*/sync/

Fix needed:
  1. Remove canvas from DB: delete from key_value where collection='system.schema'
     and name IN ('canvas', 'canvas_stark'); also update core.extension config.
  2. Remove canvas from sync config YAMLs if present.
  3. Run lando crx after.
  4. Then re-test: 3pd astro-forms module --install (verify lando drush en succeeds)
  5. Verify test route: http://sandbox-glazed.lndo.site/hudx-test/<appname>

After canvas fix, NEXT: React starter cleanup (see "NEXT" section below).


## WHERE WE LEFT OFF (pick up here next session)

### ✓ 0.starter-astro — DONE (2026-03-27)
  - Clean read-only Astro SSG starter, no backend, no DB
  - HAProxy bracket issue documented + fixed in template
  - version: Date.now() in create-module.js
  - node_modules removed from starter-template, .gitignore added
  - .env.example uses generic placeholder URL
  - index.astro has clear YOUR_CONTENT_TYPE placeholder with dev guidance
  - PROMPT.txt complete including mandatory checklist

### ✓ 0.starter-astro-forms — DONE + FULLY TESTED (2026-03-27)
  - Astro SSG + Express + SQLite (better-sqlite3 ^11.0.0)
  - server/server.js: 127.0.0.1 bind, explicit CORS, GET+POST /api/submissions
  - server/db/database.js: singleton, WAL mode, auto-schema on startup
  - server/db/schema.sql: submissions table
  - CLI: 3pd astro-forms app <name> / 3pd astro-forms module [--install]
  - Tested end-to-end: form submits, SQLite persists, list renders
  - concurrently added — npm run dev starts both Express + Astro in one command
    (dev:server and dev:astro available individually for debugging)
  - 3pd help rewritten: workflow steps at top, starter kit descriptions, 3PD vs --install explanation
  - PROMPT.txt complete
  - NOTE: better-sqlite3 must be ^11.0.0+ for Node 23 (v9.x fails to compile)
  - .env.production added to starter-template — Astro/Vite uses this automatically
    during `astro build`, baking in blank PUBLIC_* vars for same-origin production use.
    .env keeps full dev URLs. No manual blanking needed before --install.
  - CORS configured in web/sites/default/services.yml to allow localhost:4321
    so the Astro dev server can fetch Drupal JSON:API cross-origin during development.
  - Submissions 404 on Drupal test route is EXPECTED on Lando — Express runs on the
    host Mac, not inside the container. Reverse proxy gap closes in real deployment.
    Express must be running (via PM2 in production) for /api/ to connect.

### REPO STRUCTURE — decided (2026-03-27)

Standalone starter repos live as siblings at the Drupal root level:

  SANDBOX-glazed/
    web/
    3pd-astro/           ← standalone repo (3PD devs clone this)
    3pd-astro-forms/     ← standalone repo
    3pd-react/           ← standalone repo
    3pd-ide/             ← internal workspace (active apps, --install tooling)

Each starter repo is fully self-contained: starter-template/, cli/, create-module.js,
package.json (with 3pd bin), PROMPT.txt. No shared CLI across repos.

3pd-ide stays as the internal monorepo for running --install, managing active apps,
and Drupal integration. 3PD devs never see it.

Option A (all inside 3pd-ide/) was rejected — nested git repos create friction
and 3PD devs would see sibling starters they don't need.

CLI refactor needed: each repo gets its own trimmed cli/ (single framework only).
Do this when extracting each starter into its own repo.


### ✓ 0.starter-react — DONE (2026-03-27)
  - PostgreSQL → SQLite (better-sqlite3 ^11.0.0)
  - version: Date.now() in libraries.yml
  - Fixed drush commands: lando drush en + php:eval fallback + lando crx
  - Fixed Express cors() to explicit options, bind to 127.0.0.1
  - Added test route (routing.yml + HudxTestController.php) to create-module.js
  - Added .env.production with blank VITE_API_BASE_URL
  - Consolidated to single root .env (removed server/.env.example)
  - Removed PostgreSQL setup from react-app.js (no createdb/psql)
  - Rewrote PROMPT.txt to match Astro starter standard
  - NOTE: React uses VITE_ prefix (not PUBLIC_) for client-exposed env vars


## CONTENT TYPES ON THIS SITE

  drag_and_drop_page          (nid 4: "Sandbox - DXPR")
  basic_page_layout_builder   (nid 6: "Sandbox - Layout Builder",
                               nid 10: "SANDBOX - 3PD Depot",
                               nid 11: "SANDBOX - Stracat")

There is NO `page` content type with nodes. The starter template's
`node/page` default fetch URL needs to be updated to `basic_page_layout_builder`
for this specific site.


## 3PD WORKFLOW (third-party developer process)

3PD dev (no access to /web Drupal codebase):
  1. Clone their framework's starter repo (e.g. 0.starter-astro)
  2. Run: 3pd astro app <feature-name>
  3. Build the feature in their app folder
  4. Run: 3pd astro module   (no --install — packages module subfolder only)
  5. Push feature branch containing the module subfolder
  6. Open PR to main repo

Drupal owner (this repo):
  1. Pull the feature branch
  2. Run: 3pd astro module --install   (copies to /web/modules/custom + enables)
  3. Run: lando crx
  4. Place block in Layout Builder

The module subfolder is the deliverable. The app src/ is the development environment.


## REVISIT IN CI/CD DISCUSSION

### Test route in generated modules (/hudx-test/<appname>)
Every generated Drupal module includes a HudxTestController + routing.yml
that creates a /hudx-test/<appname> page for development testing.
This route is gated by 'access content' permission only.

Before production deployment this needs one of:
  A) Remove routing.yml + src/Controller/ from the module before deploy
  B) Gate route behind a stricter permission (e.g. 'administer site configuration')
  C) Disable via environment-aware config (if dev/prod environments are split)

The right answer depends on the CI/CD pipeline design.
Flag this when starting the CI/CD discussion.


## KNOWN PENDING ISSUES

- `canvas` module: FIXED (2026-03-27). Removed from DB active config + system.schema
  key_value + all canvas.* sync config YAMLs. `lando drush en` now works cleanly.
- React create-module.js still has old drush path (cd /app/web && drush).
  Fix when next touching React starter. See 0.starter-react/PROMPT.txt.
- React server.js: bare cors() + missing 0.0.0.0 binding.
  Fix when next touching React starter.
- Docker Desktop update recommended (shown in lando output warnings).
- OPcache CLI not yet enabled (was causing startup issues — skip for now).


## README.md

Created README.md at the repo root (2026-03-28) for lower-level devs setting up locally.
Covers: prerequisites, first-time setup, settings.php DB block, lando crx explanation,
troubleshooting, project structure, 3pd CLI usage, content types, Pantheon info.

KEEP THIS UPDATED. Any time the setup process changes — new prereqs, new lando commands,
new content types, Pantheon URL changes, CLI command changes — update README.md to match.


## SESSION: 2026-03-28 — Pantheon setup

### Goal
Stand up a shared Pantheon Dev environment so 3PD devs can point their apps at a
real live Drupal JSON:API endpoint (instead of running Lando locally).

### What was done

1. Created Pantheon site "3PD IDE" (machine name: 3-pd-ide) using Drupal (Composer Managed) upstream.
   Site ID: 5921a711-db26-437b-93a3-6c5aa093be3d
   Dev URL: https://dev-3-pd-ide.pantheonsite.io

2. Created .github/workflows/deploy-pantheon.yml — pushes main → Pantheon master on every push to main.
   Uses two GitHub repo secrets:
     PANTHEON_SSH_KEY  — RSA private key (ed25519 not supported by Pantheon)
     PANTHEON_GIT_URL  — ssh://codeserver.dev.5921a711-db26-437b-93a3-6c5aa093be3d@codeserver.dev.5921a711-db26-437b-93a3-6c5aa093be3d.drush.in:2222/~/repository.git

3. Added pantheon.yml (NOT pantheon.upstream.yml — that's reserved for upstream):
     api_version: 1
     web_docroot: true
     build_step: true
     php_version: 8.3
     database:
       version: 10.6

4. Fixed settings.php — wrapped lando DB credentials in isset($_ENV['LANDO_APP_NAME'])
   check so they only apply locally. Added Pantheon settings include at bottom:
     if (file_exists('/var/www/html/sites/default/settings.pantheon.php')) {
       require '/var/www/html/sites/default/settings.pantheon.php';
     }

5. Exported database as db/3pddb5.sql.gz (stripped MariaDB 11.x header line that
   breaks MariaDB 10.6). Successfully imported to Pantheon via:
     terminus import:database 3-pd-ide.dev https://raw.githubusercontent.com/sweimer/sandbox-glazed/main/db/3pddb5.sql.gz

6. SSH keys for Pantheon (RSA only — ed25519 not supported):
   - pantheon_deploy  (RSA) — for GitHub Actions
   - pantheon_local   (RSA) — for local terminus/drush commands
   Both added via `terminus ssh-key:add`.
   Also added id_samhsa_rsa_m1.pub as fallback local key.
   Run `ssh-add ~/.ssh/pantheon_local` before using terminus remote:drush.

### SESSION: 2026-03-28 — Pantheon build debugging

#### Fixes applied this session

1. webform version — composer.json changed from `6.3.x-dev` to `6.3.0-beta8`
   - 6.3.x-dev required a git clone on Pantheon (timeout)
   - 6.3.0-beta8 is a tagged dist release (zip download, no git clone)
   - minimum-stability changed from `stable` to `beta` (required for beta packages;
     prefer-stable: true kept so everything else stays stable)
   - composer.lock was gitignored — removed from .gitignore and committed so Pantheon
     uses exact pinned versions during composer install

2. Lando SSL fix — .lando.yml updated
   - After a Docker Desktop update, appserver MariaDB client upgraded to 11.8.3
   - 11.8.3 client requires SSL by default; MariaDB 10.6 server has no SSL configured
   - Fix: build_as_root step drops /etc/mysql/conf.d/nossL.cnf with [client] skip-ssl
   - Also added --skip-ssl to crx mysql command as backstop
   - REQUIRES: lando rebuild -y (build_as_root only runs at image build time)
   - After rebuild: lando crx to pre-warm

3. Repo cleanup
   - Removed pantheon.upstream.yml (trailing space in filename, invalid file,
     was being pushed to Pantheon and likely confusing build pipeline)
   - Removed drupal/ directory (stray copy of hudx_react_poc_09 at repo root,
     real location is web/modules/custom/)
   - Removed invalid `composer_version: ^2` key from pantheon.yml (Pantheon
     rejected the push — that key is not supported)

4. README.md written and pushed to GitHub — see "README.md" section above

#### SESSION: 2026-03-28 (continued) — Pantheon deploy pipeline fixed

##### Composer timeout — SOLVED
Root cause: Pantheon sandbox tier was timing out running composer install.
Solution: Option B — run composer install in GitHub Actions, push pre-built
artifact (including vendor/) to Pantheon. build_step: false in pantheon.yml.

Changes made:
  - .github/workflows/deploy-pantheon.yml: added shivammathur/setup-php@v2
    (PHP 8.3 + Composer 2), composer install --no-dev --optimize-autoloader,
    git add -f vendor/, git commit, then push HEAD:master to Pantheon.
  - pantheon.yml: build_step: false (Pantheon no longer runs composer).
  - vendor/ stays gitignored in source repo. Only the Pantheon-bound push
    includes it. Source repo stays clean.
  - Pantheon context: this is a POC shared dev endpoint only. When project
    goes to production, code will be copy-pasted to a different Drupal
    codebase — not deployed from this repo.

##### settings.php — FIXED and committed
Was gitignored — Pantheon was showing install page because no settings.php
existed on the server to connect to the database.
  - Updated .gitignore: /web/sites/*/settings*.php →
    /web/sites/*/settings.local.php (only ignore local overrides)
  - settings.php is now committed and pushed.

##### Pantheon DB connection — FIXED
Two failed attempts before finding the right approach:
  1. Hardcoded /var/www/html path → wrong on Pantheon (web_docroot: true
     means path is /code/web). Fixed to use __DIR__.
  2. $ENV['DB_NAME'] etc. → wrong. Pantheon passes DB credentials via
     $_SERVER['PRESSFLOW_SETTINGS'] (JSON blob), not individual env vars.
  3. CORRECT FIX: require settings.pantheon.php from vendor/:
       vendor/pantheon-systems/drupal-integrations/vendored-assets/settings.pantheon.php
     This file is the official Pantheon integration — handles DB via
     PRESSFLOW_SETTINGS, sets hash_salt, trusted_host_patterns, twig cache,
     and registers PantheonServiceProvider. It ships in vendor/ which is
     now included in every Pantheon push.
     Path in settings.php: __DIR__ . '/../../../vendor/pantheon-systems/...'

##### Pantheon — FULLY WORKING (2026-03-28)

https://dev-3-pd-ide.pantheonsite.io is live and loading Drupal.
DB connected, Drupal 11.3.3, bootstrap successful (confirmed via terminus drush status).

terminus remote:drush SSH fix:
  The RSA keys (pantheon_local, id_samhsa_rsa_m1) were in the agent but the
  SSH client was only offering ED25519 keys (routed through 1Password agent).
  Fix: added IdentityFile + algorithm flags to ~/.ssh/config for *.drush.in:
    Host *.drush.in
      IdentityFile ~/.ssh/pantheon_local
      PubkeyAcceptedAlgorithms +ssh-rsa
      HostKeyAlgorithms +ssh-rsa
  terminus remote:drush now works. Use for remote drush commands going forward.

JSON:API endpoint (for 3PD devs):
  https://dev-3-pd-ide.pantheonsite.io/jsonapi
  https://dev-3-pd-ide.pantheonsite.io/jsonapi/node/basic_page_layout_builder

##### /hudx-test/ routes — NOTE
The test route feature (routing.yml + HudxTestController) was added to
create-module.js AFTER the existing modules were generated. None of the
currently installed modules have a /hudx-test/ URL:
  hudx_react___3pd_depot  — no test route
  hudx_react___3pd_poc    — no test route
  hudx_react___stracat    — no test route
  hudx_astro___poc_01     — no test route
  hudx_astro___poc_02     — no test route (astro modules not in web/modules/custom)

To get test routes on existing apps: re-run `3pd react module --install` or
`3pd astro module --install` to regenerate the module with the new template.
New apps created from this point will have /hudx-test/<appname> automatically.

#### SESSION: 2026-03-28 (continued) — Lando bootstrap deadlock fix

##### Root cause
After `lando rebuild -y`, multiple modules with external HTTP calls (ai, ai_automators,
ai_api_explorer, ai_ckeditor, ai_observability, gemini_provider, announcements_feed,
update, dxpr_builder) all tried to make DNS lookups during bootstrap. Docker's DNS
resolver hung on the first lookup, deadlocking all Apache workers simultaneously.
The crx pre-warm procedure (stop Apache → drush status → start Apache) also hung
because drush itself was blocked on the same DNS call.

##### Fix applied
1. Added to settings.php (above Lando DB block):
     $settings['http_client_config']['timeout'] = 5;
   This bounds all Guzzle HTTP requests to 5 seconds, preventing DNS hangs from
   blocking the bootstrap indefinitely.

2. Added TRUNCATE TABLE router to crx command in .lando.yml — the router table
   was not being cleared by crx, causing "No route found" errors after cache clears.

3. Added router rebuild step to crx in .lando.yml:
     drush php:eval "\Drupal::service('router.builder')->rebuild();"
   NOTE: .lando.yml changes require `lando rebuild -y` to take effect.
   Current session used manual `drush php:eval` workaround after each crx.

##### lando rebuild -y — DONE (2026-03-28)
SSL fix (build_as_root nossl.cnf) is now applied. lando crx works again.

##### IMPORTANT: next lando rebuild required
.lando.yml now has router truncate + rebuild in crx, but this requires
another `lando rebuild -y` to take effect. Until then, after any lando crx
that clears the router, run manually:
  lando drush php:eval "\Drupal::service('router.builder')->rebuild();"
OR just refresh the browser — Drupal rebuilds the router on the first web request
if the router table is empty (may show "no route" on first load, reload fixes it).

### SESSION: 2026-03-28 (continued) — Full deploy pipeline complete

#### Pantheon deploy pipeline — FULLY AUTOMATED

Every push to main now:
  1. GH Actions: composer install --no-dev → git add -f vendor/ → push to Pantheon
  2. Pantheon Quicksilver (post-deploy, auto): drush updb -y → router rebuild → drush cr
  3. GH Actions (terminus): enables any hudx_* modules present in code but not in DB

Components:
  - pantheon.yml: workflows.sync_code.after → private/scripts/post_deploy.php
  - private/scripts/post_deploy.php: Quicksilver script (drush updb + router + cr)
  - .github/workflows/deploy-pantheon.yml: terminus install + auth + pm:enable hudx_*
  - GitHub secrets: PANTHEON_MACHINE_TOKEN added (machine token, not session token)
  - ~/.ssh/config: *.drush.in block with IdentityFile + RSA algorithm flags (local only)

Content on Pantheon persists across code deploys. New hudx_* modules auto-enable.
No manual terminus commands needed after pushes.

#### astro-forms---poc-05 — deployed to Pantheon (2026-03-28)
  - Built with `3pd astro-forms module --install` from app directory
  - Fetches basic_page_layout_builder from Pantheon JSON:API (same-origin in prod)
  - Form submissions require Express backend — work in npm run dev, not in Drupal module
  - Module enabled on Pantheon: hudx_astro_forms___poc_05
  - Block available to place in Layout Builder on Pantheon
  - Test route: https://dev-3-pd-ide.pantheonsite.io/hudx-test/astro-forms---poc-05

#### Dev workflow for apps (confirmed working 2026-03-28)
  - PUBLIC_DRUPAL_BASE_URL=https://dev-3-pd-ide.pantheonsite.io in app .env
  - npm run dev: Astro at localhost:4321 fetches from Pantheon JSON:API
  - Content changes on Pantheon appear immediately in dev app on refresh
  - Form submissions: Express at localhost:3001 (dev only)

#### IMPORTANT: next lando rebuild still required
.lando.yml crx improvements (router truncate + rebuild) need `lando rebuild -y`.
Until then: after lando crx, reload browser once if "No route found" appears.

### AFTER PANTHEON IS UP (remaining tasks)

- Update .env in remaining 3pd-ide/apps/* to use Pantheon URL
- Standalone repo extraction: create 3pd-astro/, 3pd-astro-forms/, 3pd-react/
  as independent repos alongside 3pd-ide/ at the SANDBOX-glazed root
- CI/CD discussion: test route gating, --install → git commit+push for Pantheon


## SESSION: 2026-03-29 — Form persistence via Drupal DB (Option A)

### Problem
astro-forms apps deployed as Drupal modules had broken form submit + submissions
list (HTTP 404) because the module's static JS called /api/submissions on the
same origin (Drupal), but no route existed there. Express/SQLite only runs locally.

### Decision: Option A — Drupal handles the API
The Drupal module now exposes /api/submissions itself, backed by Drupal's MySQL DB.
This is the right fit for Pantheon (no separate process, same-origin, no CORS needed).
The local Express/SQLite remains for 3PD dev workflow (they don't run Drupal locally).
On --install, SQLite data is read and baked into the module as seed data.

### What was built

#### 3pd.config.json (new file, repo root)
Project-level config read by the CLI when creating new apps:
  {
    "pantheonUrl": "https://dev-3-pd-ide.pantheonsite.io",
    "defaultContentType": "basic_page_layout_builder"
  }

Both astro-forms-app.js and astro-app.js now read this on app creation:
  1. Sets PUBLIC_DRUPAL_BASE_URL automatically (no manual .env edit)
  2. Replaces YOUR_CONTENT_TYPE in index.astro automatically (no manual edit)
New apps are fully functional out of the box.

#### create-module.js — major additions (0.starter-astro-forms)

1. SQLite reader (readSqlite)
   Writes a temp .mjs script into the app dir, runs it via node to read
   server/db/app.sqlite using better-sqlite3. Reads all rows + PRAGMA table_info
   from the submissions table. Returns empty gracefully if DB doesn't exist.

2. data/seed.json
   Written into every generated module. Contains SQLite columns + all rows.
   Committed to git with the module — bakes local dev data into the deploy artifact.

3. {machineName}.install
   - hook_schema(): defines the submissions table in Drupal's schema API.
     Column types derived from SQLite PRAGMA (INTEGER→serial/int, TEXT→text big,
     DATETIME/VARCHAR→varchar 255). Falls back to hardcoded defaults if no DB.
   - hook_install(): calls _import_seed() on first enable. Drupal creates the
     table from hook_schema() before hook_install() runs.
   - _import_seed(): truncates the table, inserts all rows from seed.json.
     Called by hook_install() on first install, and by post_deploy.php on every
     Pantheon deploy to keep seed data in sync.

4. src/Controller/SubmissionsController.php
   - list(): GET /api/submissions — SELECT * ORDER BY id DESC → JsonResponse
   - create(): POST /api/submissions — INSERT name+message+created_at → 201

5. .routing.yml — updated with API routes
   - {machineName}.api_submissions_list:  GET  /api/submissions
   - {machineName}.api_submissions_create: POST /api/submissions
   No CSRF token required (JsonResponse controllers, not form submissions).

#### private/scripts/post_deploy.php — updated
Scans /code/web/modules/custom/ for any module with a data/seed.json.
For each found, calls _import_seed() via drush php:eval.
Result: every Pantheon push re-syncs seed data for all hudx forms modules.
Pattern: update local SQLite → run --install → push → Pantheon auto-seeds.

### Status: ✓ WORKING (2026-03-29)
Full cycle confirmed on Lando:
  - `3pd astro-forms app APP 10/11/12` → auto-sets Pantheon URL + content type, works immediately
  - `3pd astro-forms module --install` → builds, seeds, installs, rebuilds router
  - GET /api/submissions → returns seeded rows from Drupal MySQL
  - POST /api/submissions → inserts new row, appears in list
  - Test route: http://sandbox-glazed.lndo.site/hudx-test/astro-forms---app-12

Pushed to GitHub 2026-03-29. Pantheon deploy pending verification.

### ⚠ KNOWN ISSUE: /api/submissions route conflict
Every astro-forms module registers /api/submissions.
Installing TWO astro-forms apps on the same Drupal will cause a routing conflict.
FIX NEEDED before going beyond one active forms app:
  Option 1: Namespace the route per app: /api/{machineName}/submissions
  Option 2: One shared Drupal module handles all /api/submissions routing,
            dispatching to the correct table based on a query param or path segment.
  Option 3: Keep one forms app per site (acceptable for POC).
Route must also be updated in the Astro app's JS fetch calls to match.
Address this BEFORE applying the same pattern to the React starter.

### TODO — next session
1. ✓ 3pd help updated
2. ✓ 0.starter-astro-forms/PROMPT.txt updated
3. Verify Pantheon deploy: https://dev-3-pd-ide.pantheonsite.io/hudx-test/astro-forms---app-12
   - Drupal content loads ✓ (expected — same as poc-05)
   - GET /api/submissions returns seed row
   - POST /api/submissions works
4. ✓ Route conflict resolved (see SESSION 2026-03-29 continued below)
5. Apply lessons to 0.starter-react:
   - Same 3pd.config.json auto-config (VITE_ prefix not PUBLIC_)
   - Same .install + seed.json pattern for form persistence
   - Same namespaced route solution (APP_SLUG)


## SESSION: 2026-03-29 (continued) — Route namespacing, Twig fix, 3PD Checklist app

### Route conflict fix — APP_SLUG namespacing (✓ DONE)

All astro-forms routes are now namespaced per app: /api/{APP_SLUG}/{table}
  e.g. /api/astro-forms---3pd-checklist/checklist

Changes made:
  - starter-template/.env.example: added APP_SLUG=YOUR_APP_SLUG and PUBLIC_APP_SLUG=YOUR_APP_SLUG
  - starter-template/server/server.js: APP_SLUG from env, routes use /api/${APP_SLUG}/submissions
  - starter-template/src/pages/index.astro: fetch URLs use /api/${APP_SLUG}/submissions
  - cli/commands/astro-forms-app.js: replaces APP_SLUG placeholder with actual folder name on app creation
  - create-module.js (starter): routes in routing.yml use /api/${appName}/${tableName}
    table name detected dynamically from sqlite_master (not hardcoded to 'submissions')
    readSqlite() returns { rows, cols, tableName } — tableName used throughout

### Twig fix — Astro SSG body HTML (✓ DONE, critical)

Problem: Astro builds with interactivity produce an external JS bundle (hoisted.*.js).
Old logic only extracted body HTML from dist/index.html when NO external bundle existed.
If a bundle existed, the twig was a bare mount div → JS ran but found no DOM elements.
Result: white page on Pantheon even though module was enabled.

Fix applied to BOTH starter create-module.js AND checklist create-module.js:
  Always extract body HTML from dist/index.html. Strip <script> and <link> tags.
  Use bodyHtml as twig content regardless of whether external JS files exist.
  JS/CSS loaded via libraries.yml, not inline in twig.

  const twig = bodyHtml ? `${bodyHtml}\n` : `<div id="${mountId}"></div>\n`;

### Local create-module.js override (✓ DONE)

astro-forms-module.js now checks for a create-module.js in the current app directory
before falling back to the starter kit's generic one:

  const localCreateModulePath = path.join(cwd, 'create-module.js');
  const starterCreateModulePath = path.join(ideRoot, 'apps', '0.starter-astro-forms', 'create-module.js');
  const createModulePath = fs.existsSync(localCreateModulePath) ? localCreateModulePath : starterCreateModulePath;

This allows per-app customization (different schema, routes, controller logic) without
forking the starter kit.

### 3PD Module Checklist app — astro-forms---3pd-checklist (✓ DONE)

Interactive table tracking all installed hudx_* modules and their test status.
Uses custom create-module.js (local override) and custom server.js.

Architecture:
  - GET  /api/astro-forms---3pd-checklist/modules   — scans apps/ dir (local) or Drupal installed modules (prod)
  - GET  /api/astro-forms---3pd-checklist/checklist — all checklist rows
  - POST /api/astro-forms---3pd-checklist/checklist — upsert by module_name

ChecklistController.php (Drupal):
  - modules(): queries extension.list.module, filters hudx_* prefixes
    Reconstructs folder names: strip 'hudx_', replace _ with -
    Maps tech prefixes: hudx_astro_forms___ → 'Astro Forms', hudx_react___ → 'React', etc.
  - list(): SELECT * FROM checklist ORDER BY id
  - upsert(): SELECT-then-UPDATE-or-INSERT (Drupal DB API, no native upsert)

SQLite schema (local): server/db/schema.sql
Drupal schema: hudx_astro_forms___3pd_checklist_checklist table

UI features:
  - Sort on all columns except Tester Name (click column header, ↕/▲/▼)
  - Filters: checked state, tech type (dynamic), module name text, workflow status
  - Module name links to /hudx-test/{module_name} in new tab
  - Inline tester name text field, auto-save on blur
  - Workflow dropdown: auto-save on change (see below)

### Workflow column — replacing Status text (✓ DONE)

Column 5 changed from auto-generated text ("Tested / Not tested") to a live
workflow_status dropdown with 6 options:
  Not Tested / Tested / Returned / Approved / Ready for Deploy / Deployed

Schema change: replaced checked_at (VARCHAR) with workflow_status (VARCHAR, default 'Not Tested')
  in schema.sql, server.js, create-module.js, ChecklistController.php, and .install

Migration for Pantheon (existing DB has checked_at column):
  hook_update_8001() in .install — adds workflow_status, sets all to 'Not Tested', drops checked_at
  Run `drush updb` on Pantheon after pushing this code.

Sort key for workflow column uses ordinal order of the option list.
Filter dropdown matches all 6 workflow options exactly.
Row listener handles both checkbox change and select change via shared getRowFields() helper.
Dropdown disabled (opacity 0.5) during save, re-enabled after response.

### DB management strategy — discussed, deferred

Two paths discussed:
  Path 1: 3PD dev pull — `3pd astro-forms db pull` — downloads Pantheon SQLite to local
    Scenario A: original dev updating — just pulls latest data
    Scenario B: new dev cloning — first-time DB pull, no push needed
    No push path needed (Pantheon data flows into Drupal DB via --install seed)
  Path 2: Internal dev — `3pd db pull` — terminus db:get → import to local Drupal
    Uses terminus, requires full stack locally

Not yet built. Deferred until checklist work is complete and pushed.

## SESSION: 2026-03-29 (continued) — DB pull commands + astro-static cleanup

### ✓ hook_update_8001 applied to Pantheon (2026-03-29)
terminus remote:drush 3-pd-ide.dev -- updb -y
Renamed checked_at → workflow_status. No further updates pending.

### ✓ 3pd astro-forms db pull — DONE + TESTED (Path 1)
Pulls live data from Drupal API endpoint → local app SQLite.
  - Run from inside an astro-forms app folder
  - Reads APP_SLUG + PUBLIC_DRUPAL_BASE_URL from .env
  - Detects table name from server/db/schema.sql
  - Fetches GET {drupalUrl}/api/{appSlug}/{tableName}
  - Always wipes app.sqlite + stale WAL/SHM before recreating (avoids SQLITE_IOERR_SHORT_READ)
  - Runs better-sqlite3 via temp script from app's own node_modules
  - After pull: must restart dev server (kill port 3001 first if already running)
    lsof -ti:3001 | xargs kill -9 && npm run dev
  - Success message updated to include restart instructions

### ✓ 3pd db pull — DONE + TESTED (Path 2)
Pulls full Pantheon DB → local Lando Drupal instance.
  - Run from SANDBOX-glazed root (anywhere in project)
  - terminus backup:create → terminus backup:get → lando db-import → lando crx
  - File must be inside project dir (not /tmp) — lando db-import only sees mounted project
  - Uses SANDBOX-glazed/tmp/ as staging dir, cleaned up before download and after import
  - /tmp/ added to .gitignore
  - Relative path passed to lando db-import: tmp/pantheon-db.sql.gz

### ✓ 0.starter-astro → 0.starter-astro-static rename (2026-03-29)
  - Renamed folder: apps/0.starter-astro → apps/0.starter-astro-static
  - Fixed twig extraction bug in create-module.js (always extract bodyHtml, strip
    script/link tags — bare mount div is now the fallback, not the default)
  - Added router rebuild step after lando crx in internal mode
  - Updated all CLI path references: astro-app.js, astro-module.js, doctor.js, list.js
  - Updated PROMPT.txt: title, folder path, twig description, Case A/B table

### WHERE WE LEFT OFF (2026-03-29 continued)

→ React starter refactoring COMPLETE. See SESSION 2026-03-29 (continued) below.


## SESSION: 2026-03-29 (continued) — React starter refactoring

### ✓ 0.starter-react — FULLY REFACTORED (2026-03-29)

Applied all lessons from 0.starter-astro-forms to the React starter:

#### starter-template changes
  - schema.sql: renamed table test_entries → submissions, column text_value → message
  - .env.example: added APP_SLUG=YOUR_APP_SLUG and VITE_APP_SLUG=YOUR_APP_SLUG
  - server/server.js: reads APP_SLUG from env, routes namespaced /api/${APP_SLUG}/submissions
  - server/routes/testRoutes.js: DELETED. Replaced by server/routes/submissions.js
  - server/routes/submissions.js: GET /submissions + POST /submissions (returns 201 + row)
  - server/db/schema.sql: submissions(id, message, created_at)
  - src/pages/Home.jsx: reads VITE_APP_SLUG, API calls use /api/${APP_SLUG}/submissions
  - README.md: 3PD dev guide (prerequisites, setup, db pull step, dev workflow, packaging,
    data flow table, project structure, notes on VITE_ prefix + MemoryRouter)
  - Stray files deleted: react-module.js (root), server/.env.example

#### create-module.js — rewritten (same pattern as astro-forms)
  - readSqlite(): temp .mjs script → reads submissions table via better-sqlite3
  - data/seed.json: {table, columns, rows} from SQLite
  - {machineName}.install: hook_schema(), hook_install(), _import_seed()
    drupalTableName = ${machineName}_${tableName} (e.g. hudx_react___my_app_submissions)
  - SubmissionsController.php: list() GET + submit() POST for /api/{appName}/{table}
  - HudxTestController.php: test page (unchanged)
  - routing.yml: test route + namespaced GET/POST API routes
  - Twig: bare <div id="${mountId}"></div> (React always needs external bundles — no body HTML extraction)
  - --install mode: router rebuild after lando crx

#### CLI — react-app.js
  - loadProjectConfig() reads 3pd.config.json
  - setupEnvFile() injects APP_SLUG and VITE_APP_SLUG from folder name

#### CLI — react-db-pull.js (new)
  - Parallel to astro-forms-db-pull.js
  - Reads APP_SLUG + VITE_DRUPAL_BASE_URL (or VITE_API_BASE_URL) from .env,
    falls back to pantheonUrl in 3pd.config.json
  - Detects table from server/db/schema.sql regex
  - Fetches GET {drupalUrl}/api/{appSlug}/{table}
  - Always wipes app.sqlite + WAL/SHM before recreating
  - After pull restart hint: lsof -ti:4000 | xargs kill -9 && npm run dev
  - Wired as: 3pd react db pull

#### PROMPT.txt — updated
  - Directory structure updated (new files, deleted files)
  - CLI integration: added 3pd react db pull
  - How react app works: 3pd.config.json auto-config documented
  - How react module works: seed.json, .install, SubmissionsController, drupalTableName
  - Express routes: APP_SLUG namespacing documented
  - ENV vars: APP_SLUG, VITE_APP_SLUG documented
  - Known issues: 8 new entries (APP_SLUG, drupalTableName, db pull, seed pattern)
  - Mandatory checklist: 5 new items (router rebuild, APP_SLUG routes, seed, controller, drupalTableName)

#### 3pd help — updated
  - Added: 3pd react db pull   Pull live data from Drupal/Pantheon into local SQLite

### WHERE WE LEFT OFF

→ React starter refactoring complete. Next up (when resuming):
   - Test 3pd react app + 3pd react module --install end-to-end
   - Verify seed.json generation + hook_install() seeds Drupal table correctly
   - Update .env in remaining 3pd-ide/apps/react---* to use Pantheon URL
   - Standalone repo extraction: create 3pd-react/ as independent repo


## SESSION: 2026-03-29 (continued) — react---3pd-ai-helper, embed-request app, starter kit repos

### ✓ react---3pd-ai-helper — FULLY WORKING on Pantheon (2026-03-29)

First real React app using the new create-module.js pattern (PHP controllers, seed data).
Two features: Generate Code (AI) + Create Page in Drupal.

#### What was built / fixed

1. GenerateController.php (generated by create-module.js)
   PHP parse error on first deploy: JS template literal `\\$variable` → `\$variable` in PHP.
   Root cause: JS only interpolates `${...}` — bare `$var` in a template literal is safe.
   Fix: changed all `\\$varName` → `$varName` in PHP templates inside create-module.js.
   PHP namespace separators (\\Drupal, \\Exception, \\PDO) keep the double backslash — correct.

2. Pantheon API key delivery
   Pattern used: Pantheon Secrets dashboard (Runtime + Web scope) + settings.php:
     if (isset($_ENV['PANTHEON_ENVIRONMENT']) && function_exists('pantheon_get_secret')) {
       $anthropic_key = pantheon_get_secret('ANTHROPIC_API_KEY');
       if (!empty($anthropic_key)) { putenv('ANTHROPIC_API_KEY=' . $anthropic_key); }
     }
   pantheon_get_secret() is a platform function — no Drupal module required.
   Key also accessible directly via getenv() on Pantheon when Runtime+Web scope is set.

3. Local API key: settings.local.php
   Created /web/sites/default/settings.local.php (gitignored):
     putenv('ANTHROPIC_API_KEY=sk-ant-...');
   settings.php uncommented the settings.local.php include block.
   This pattern keeps keys out of git for local dev.

4. HTTP timeout fix
   settings.php `$settings['http_client_config']['timeout']` changed 5 → 30.
   5 seconds is too short for Anthropic API calls — was causing cURL timeout on Pantheon.

5. Content type fix (Create Page)
   AI-generated code was posting to node/basic_page_layout_builder which has no `body` field.
   Fix: switched to node--page (has body field, accepts HTML markup).
   Home.jsx: updated JSON:API endpoint and type string.

6. Mixed content / production URL fix
   .env had HTTP Lando URL baked into VITE_DRUPAL_BASE_URL — served over HTTPS Pantheon = blocked.
   Fix: .env → VITE_DRUPAL_BASE_URL=https://sandbox-glazed.lndo.site
         .env.production → VITE_DRUPAL_BASE_URL= (blank, same-origin in production)

7. SQLite missing columns
   history table predated schema change — `title` and `node_url` columns missing.
   Fixed with ALTER TABLE history ADD COLUMN (one-time manual migration).

8. Module info.yml configure link
   Added `configure: ${machineName}.test_page` to .info.yml template in create-module.js.
   Gives the module a "Configure" link on the Extend page for quick test access.

#### DB install/uninstall cycle (known pattern)
When schema changes after deployment, terminus drush pm:uninstall + pm:enable
recreates the table from hook_schema(). Data is lost — coordinate schema changes early.

#### react-module.js local override
react-module.js (like astro-forms-module.js) checks for a local create-module.js
in the app directory before falling back to the starter kit's generic one.
Apps that need custom controllers (e.g. react---3pd-ai-helper with GenerateController
+ HistoryController) use their own create-module.js without forking the starter kit.


### ✓ react---3pd-embed-request — BUILT (2026-03-29)

New React app for collecting Smart Embed and Stand-Alone Link submissions from 3PD devs.
Single form with a type radio (embed / link) — one app, not two, because the fields overlap.

Schema (server/db/schema.sql):
  submitter info: name, email, organization
  type: 'embed' or 'link'
  core: title, description, justification, url, go_live_date
  embed-only: code_snippet, data_sensitivity, collects_user_data, requires_auth
  governance: requested_placement
  admin: status (Pending/Approved/Returned/Deployed), admin_notes, created_at

API (server/routes/submissions.js):
  GET  /submissions         — all rows ordered by id DESC
  POST /submissions         — validated insert (required: type, title, url, submitter_name, submitter_email)
  PATCH /submissions/:id    — admin update (status + admin_notes only)

UI (src/pages/Home.jsx):
  fieldsets: Your Information / Request Type / Submission Details / Governance
  code_snippet and requires_auth fields are shown only for embed type
  Success screen shown after submit (no page reload)
  Returns to form with "Submit Another" button

App nav removed from App.jsx — single route, no nav needed.

App deployed to Lando with: 3pd react module --install
Linked from react---3pd-depot Home.jsx (both Smart Embeds and Stand-Alone Properties cards).


### ✓ react---3pd-depot — 6th card added (2026-03-29)

Added "3PD Module Manager" card as the last card in the grid.
Buttons from above the "Choose Your Build Path" divider were removed (cleaned up header).
Card: border-dark, bi-house-heart icon, links to /hudx-test/astro-forms---3pd-checklist
      and /admin/content.
Delay class on Stand-Alone Properties card updated to dxpr-delay-6 (was unset).


### ✓ Starter kit GitHub repos — 3 public repos created (2026-03-29)

All three starter kits published as standalone public GitHub repos.
Source: starter-template/ subdirectory contents only — internal CLI tools not included.

  https://github.com/sweimer/3PD---React-Starter-Kit
    Local: /Users/29457/Documents/Sites/3pd-react-starter-kit/
    README: targets new 3PD devs — HUDX context, when-to-use, setup steps, structure, gotchas

  https://github.com/sweimer/3PD---Astro-Static-Starter-Kit
    Local: /Users/29457/Documents/Sites/3pd-astro-static-starter-kit/
    README: same format — static display apps, JSON:API fetch, packaging for Drupal

  https://github.com/sweimer/3PD---Astro-Forms-Starter-Kit
    Local: /Users/29457/Documents/Sites/3pd-astro-forms-starter-kit/
    README: same format — form + data persistence, Express + SQLite, db pull workflow

READMEs target new 3PD developers who have no Drupal or HUDX background.
Cross-links between READMEs guide devs to the right starter kit for their use case.

IMPORTANT: These repos contain only starter-template/ contents.
The internal PROMPT.txt and create-module.js live only in 3pd-ide/apps/0.starter-*/
and are not published to the public repos.


### ✓ react db pull SSL fix (2026-03-29)

Node.js rejects self-signed SSL certs from Lando (*.lndo.site).
Fix in cli/commands/react-db-pull.js:
  const isLocal = apiUrl.includes('lndo.site') || apiUrl.includes('localhost');
  if (isLocal) process.env.NODE_TLS_REJECT_UNAUTHORIZED = '0';
  const res = await fetch(apiUrl);
  if (isLocal) process.env.NODE_TLS_REJECT_UNAUTHORIZED = '1';
Same pattern should be applied to astro-forms-db-pull.js if Lando pull is needed there.


### WHERE WE LEFT OFF (end of 2026-03-29 session)

Completed:
  ✓ react---3pd-ai-helper working on Pantheon (Generate + Create Page)
  ✓ react---3pd-embed-request built and installed on Lando
  ✓ react---3pd-depot updated (6th card, header cleanup)
  ✓ 3 starter kit GitHub repos published with developer-facing READMEs

Pending:
  - Deploy react---3pd-embed-request to Pantheon (3pd react module --install from Lando context)
  - Deploy react---3pd-depot updated build to Pantheon
  - Build 0.starter-vanillajs-static and 0.starter-vanillajs-forms (deferred)
  - Build Angular starter (deferred)
  - Option 2 Page Builder POC (deferred — drag-and-drop React builder with AI assist)
  - Update react---3pd-depot Option 2 button link when Option 2 is ready
  - Gate /hudx-test/* routes before any production deployment
  - hook_views_data() support in module generator (roadmap item)
  - react---3pd-depot Card 5: rename to "3PD Design System" — design guidance for 3PD devs.
    Button links to Storybook (not yet built). Placeholder button until Storybook is ready.
    Card 4 = Smart Embeds + Stand-Alone Links combined (react---3pd-embed-request)
    Card 5 = 3PD Design System (Storybook link, coming soon)
    Card 6 = 3PD Module Manager (astro-forms---3pd-checklist) — stays last


## SESSION: 2026-03-30 — Drupal Dev Assets + react---3pd-depot card restructure

### ✓ react---3pd-depot — Cards 4/5/6 restructured

  Card 4: "3PD Stand-Alone / Smart Embeds" (border-primary, bi-link-45deg)
          merged Smart Embeds + Stand-Alone Properties into one card
          links to react---3pd-embed-request
  Card 5: "3PD Design System" (border-info, bi-palette)
          new card for design guidance / Storybook
          button currently links to USWDS Storybook (placeholder until we build ours)
  Card 6: "3PD Module Manager" (border-dark, bi-house-heart) — unchanged

### ✓ Drupal Dev Assets — Bootstrap + DXPR theme in local dev (all starters)

Goal: 3PD devs building apps locally see Bootstrap + DXPR theme styles.
      When the app is packaged as a Drupal module, those style references must
      NOT be included — they would duplicate styles already loaded by Drupal.

#### Solution

Two generated files (gitignored, never committed) in each app's public/:
  public/drupal-dev-styles.css  — Bootstrap + DXPR theme CSS concatenated
  public/drupal-dev-scripts.js  — Bootstrap bundle JS (DXPR scripts excluded,
                                   require Drupal.behaviors + drupalSettings globals)

Bootstrap Icons loaded from CDN (jsdelivr.net) to avoid relative font path issues.

#### Dev-only injection (no leakage into Drupal modules)

React (Vite plugin):
  vite.config.js has a drupalDevAssets() plugin with apply: 'serve'
  → runs ONLY during vite dev, completely skipped during vite build
  → injects Bootstrap Icons CDN + /drupal-dev-styles.css + /drupal-dev-scripts.js into index.html

Astro (import.meta.env.DEV):
  Layout.astro uses {isDev && <link ...>} / {isDev && <script ...>}
  → import.meta.env.DEV is statically replaced with false during astro build
  → dead code — tree-shaken out, never reaches the Drupal module twig template

#### Files changed

  apps/react---3pd-depot/vite.config.js            — added drupalDevAssets() plugin
  apps/react---3pd-depot/index.html                — removed hardcoded CSS links
  apps/0.starter-react/starter-template/vite.config.js   — added drupalDevAssets() plugin
  apps/0.starter-astro-static/starter-template/src/layouts/Layout.astro — isDev conditional
  apps/0.starter-astro-forms/starter-template/src/layouts/Layout.astro  — isDev conditional
  All 3 starter-template .gitignore files — added drupal-dev-styles.css + drupal-dev-scripts.js

#### New CLI commands

  3pd styles sync     — generates dev assets in ALL apps + starter templates
                        Monorepo: reads from web/themes/contrib/ directly (fast, offline)
                        Standalone: fetches from PUBLIC_DRUPAL_BASE_URL / VITE_DRUPAL_BASE_URL in .env
                        Wired into react-app.js / astro-app.js / astro-forms-app.js
                        so new apps always get the files automatically on creation.

  3pd styles refresh  — regenerates dev assets for the CURRENT app only
                        Run from inside an app dir when the Drupal theme changes.
                        Then restart npm run dev to pick up changes.

#### New shared module

  starter-scripts/cli/shared/drupalDevAssets.js
    Exports buildDrupalDevAssets({ drupalRoot, cwd })
    Used by both styles-sync.js (all apps) and styles-refresh.js (current app).
    DRY — no duplicated build logic.

#### Monorepo vs standalone detection

  Checks: fs.existsSync(drupalRoot + '/web/themes/contrib/dxpr_theme')
  Monorepo → reads CSS/JS from local theme files (no network, always current)
  Standalone → fetches aggregated CSS/JS from Drupal URL in app's .env

#### CSS files included (monorepo mode)

  bootstrap5 bootstrap.min.css
  dxpr_theme variables.css, bootstrap-5.css, bootstrap-theme.css,
            layout.css, typography.css, forms.css, helper-classes.css, animations-dxpr.css

#### JS files included (monorepo mode)

  bootstrap5 bootstrap.bundle.min.js only
  (DXPR JS excluded — requires Drupal.behaviors, drupalSettings, once globals)

### ✓ PROMPT.txt updates (all 3 starters)

Added "DRUPAL DEV ASSETS" section to:
  0.starter-react/PROMPT.txt
  0.starter-astro-static/PROMPT.txt
  0.starter-astro-forms/PROMPT.txt

Documents: how the files are injected, why they're dev-only, what's included/excluded,
and the 3pd styles refresh / 3pd styles sync commands.

### WHERE WE LEFT OFF (2026-03-30)

Pending (carried forward):
  - Deploy react---3pd-embed-request to Pantheon
  - Deploy react---3pd-depot updated build (new cards + styles plugin) to Pantheon
  - Build 0.starter-vanillajs-static and 0.starter-vanillajs-forms (deferred)
  - Build Angular starter (deferred)
  - Option 2 Page Builder POC (deferred)
  - Gate /hudx-test/* routes before production deployment


## SESSION: 2026-03-30 (continued) — Smart Embed starter + SC Training POC

### 0.starter-smart-embed — BUILT (2026-03-30)

New starter kit for embedding external or legacy apps into Drupal via a
resizable iframe using postMessage. No React, no build step, no npm.

Architecture:
  - Two modes set in embed.config.json:
      embedUrl   → iframes an external live URL (Option A)
      staticDir  → copies local static files to web/embeds/<slug>/, iframes internally (Option B)
  - Child-side snippet (snippet/hudx-resize.js): drop into any app, sends scrollHeight on
    load/resize/popstate/hashchange via window.parent.postMessage({ type: 'hudx-resize', height })
  - Parent-side Drupal block: Drupal.behaviors listener, sets iframe.style.height
  - Fallback height (px) shown until first postMessage received

New files:
  apps/0.starter-smart-embed/create-module.js
  apps/0.starter-smart-embed/snippet/hudx-resize.js
  apps/0.starter-smart-embed/PROMPT.txt
  starter-scripts/cli/commands/embed-create.js
  starter-scripts/cli/commands/embed-module.js

Modified files:
  starter-scripts/cli/index.js   — added embed namespace + help
  starter-scripts/cli/shared/frameworks.js — added embed: 'embed---'

CLI commands:
  3pd embed create <name>              Create embed app folder + snippet
  3pd embed create <name> --url <url>  Pre-fill the embed URL
  3pd embed module                     Generate Drupal block module (3PD)
  3pd embed module --install           Generate + install (HUDX)

Generated Drupal module:
  Block plugin using Markup::create() — IMPORTANT: plain #markup strips iframes
  via Drupal XSS filter. Must use Markup::create() for any block rendering iframes.
  JS listener uses Drupal.behaviors + once() — attaches only once per element.

### Cloudflare X-Frame-Options — why static hosting is needed
  apps like the SC training tool are hosted on AWS S3 + Cloudflare which sets
  X-Frame-Options blocking iframes. Option A (external URL) is blocked.
  Option B (static files self-hosted) works because same-domain iframes bypass
  all X-Frame-Options restrictions.

### staticDir mode (Option B)
  create-module.js gains staticDir support:
  - Resolves path relative to 3pd-ide/ root (ideRoot)
  - Copies files to web/embeds/<slug>/ (served directly by Apache, bypasses PHP)
  - Copies hudx-resize.js into deployed root
  - Injects <script src="hudx-resize.js"></script> before </body> in all .html files
  - Sets iframe src to /embeds/<slug>/

  Static files in web/embeds/ are NOT gitignored (needed for Pantheon).
  Source files in 3pd-ide/smart-embeds/ are the version-controlled copy.

### embed---sc-training POC — WORKING (2026-03-30)

Source: /Users/29457/Documents/Sites/0.apps/sc-training-tool/app (Adobe Muse export)
        Copied to: 3pd-ide/smart-embeds/sc-training/ (380 files, 36MB)
Deployed: web/embeds/sc-training/ (written by --install)
Module: web/modules/custom/hudx_embed___sc_training/
Test route: http://sandbox-glazed.lndo.site/hudx-test/embed---sc-training

#### Critical fix: Adobe Muse verticalspacer infinite loop
Muse pages use <div class="verticalspacer" style="height: calc(-1359px + 100vh)">
to pin the footer to the viewport bottom. Inside an iframe, 100vh = iframe height.
This creates a circular loop: spacer expands → scrollHeight grows → iframe grows → repeat.
Result: iframe is always ~8770px regardless of page content.

Fix applied to hudx-resize.js:
  When inside an iframe, inject CSS: .verticalspacer { display: none !important; }
  This removes the spacer from layout before measuring scrollHeight.
  Footer flows naturally after main content. Height reflects real content per page.

#### Critical fix: Drupal XSS filter strips iframe from #markup
  Drupal's #markup runs through Xss::filter() which removes <iframe> tags.
  Fix: use Markup::create() to mark the HTML as trusted.
  Applied to both the live module and create-module.js template.

#### lando drush breakage (2026-03-30)
  lando drush pm:enable / pm:uninstall failing with:
    "exec: vendor/bin/drush: no such file or directory"
  Workaround: lando php /app/vendor/bin/drush.php pm:enable <module> -y
  Root cause unknown — affects all starters' create-module.js tryExec calls.
  TODO: investigate and fix lando drush path resolution.

### WHERE WE LEFT OFF (end of 2026-03-30 session)

In progress:
  - embed---sc-training POC working locally, user testing per-page resize behavior

Pending:
  - Verify per-page resize (contents → module 2.1 → 2.2 etc.)
  - Verify quiz page resize after accordion/answer interactions
  - Fix lando drush path breakage in create-module.js tryExec (affects all starters)
  - web/embeds/ Pantheon deploy strategy (same as vendor/ — commit in GH Actions push)
  - Deploy react---3pd-embed-request to Pantheon
  - Deploy react---3pd-depot to Pantheon
  - Gate /hudx-test/* routes before production deployment


## SESSION: 2026-03-30 (continued) — 3PD Checklist polish + module UX

### ✓ 3PD Checklist — Module column now shows full display name

  Previously: Module column showed stripped slug (e.g. "app-12")
  Now: pulls $info['name'] directly from the module's .info.yml
       (e.g. "3PD IDE - Astro Forms App 12")

  Changed:
    web/modules/custom/hudx_astro_forms___3pd_checklist/src/Controller/ChecklistController.php
    3pd-ide/apps/astro-forms---3pd-checklist/create-module.js (source template)

  Removed the slug_prefix stripping logic. $display_name = $info['name'] ?? $app_name.

### ✓ 3PD Checklist — Default sort changed to Tech A→Z

  sortCol initialised to 'tech' (was empty string = no sort).
  Changed in: 3pd-ide/apps/astro-forms---3pd-checklist/src/pages/index.astro
  Rebuilt and redeployed via create-module.js --install.

### ✓ Drupal Extend page — "Configure" button relabeled to "View" for hudx_* modules

  The configure: key in .info.yml always renders as "Configure" in Drupal core —
  this is hardcoded in ModulesListForm.php and cannot be overridden via info.yml.

  Fix: hook_form_system_modules_alter() in the checklist .module file.
  Targets only modules whose machine name starts with hudx_.
  All other modules on the Extend page retain the standard "Configure" label.

  Changed: web/modules/custom/hudx_astro_forms___3pd_checklist/hudx_astro_forms___3pd_checklist.module

### ✓ configure: key added to all installed modules and all starter templates

  Added configure: <machineName>.test_page to .info.yml for 6 live modules:
    hudx_astro_forms___3pd_checklist, hudx_astro_forms___app_12,
    hudx_react___3pd_depot, hudx_react___3pd_embed_request,
    hudx_react___3pd_poc, hudx_react___stracat

  Added to all 3 starter create-module.js templates (generates for future modules):
    0.starter-react, 0.starter-astro-static, 0.starter-astro-forms

### ✓ Smart Embed — own repo decision

  Decision: 0.starter-smart-embed does NOT need its own git repo.
  Reason: React/Astro starters need repos because developers clone a full npm
  project scaffold. Smart embed has no build step, no package.json, no npm.
  The CLI generates everything on the fly and copies only hudx-resize.js from the
  starter. A separate repo would add overhead with nothing to gain.

### WHERE WE LEFT OFF (end of 2026-03-30 session 2)

Completed this session:
  - 3PD Checklist: Module column shows full name from .info.yml
  - 3PD Checklist: Default sort Tech A→Z
  - Drupal Extend: "View" button for all hudx_* modules
  - configure: key on all live modules + all starter templates

Pending:
  - Discuss web/embeds/ Pantheon deploy strategy (next conversation)
  - Verify per-page resize on embed---sc-training (user still testing)
  - Fix lando drush path breakage in create-module.js tryExec (affects all starters)
  - Deploy react---3pd-embed-request to Pantheon
  - Deploy react---3pd-depot to Pantheon
  - Gate /hudx-test/* routes before production deployment


## SESSION: 2026-03-30 (continued) — CloudFront POC for smart embed

### Context: why CloudFront instead of static hosting in Drupal repo

  The trainings apps on hudexchange.info are hosted on S3 + CloudFront.
  The prod CloudFront distribution sets X-Frame-Options blocking iframes.
  The HUDX team cannot change this now due to resourcing constraints.
  Goal: test the embedUrl mode (no files in Drupal repo) using a POC CloudFront
  distribution we control, without touching prod.

### ✓ S3 POC prefix created

  Copied trainings/ prefix within the same bucket:
    s3://sites.hudexchange.info/trainings/ → s3://sites.hudexchange.info/trainings-poc/

  Note: the copy nested the directory one level deeper than intended:
    trainings-poc/trainings/service-coordinators.../   (not trainings-poc/service-coordinators.../)
  Acceptable for POC. Re-copy with correct structure if moving to prod.

  Bucket already had a public read policy (/* wildcard) — no ACL changes needed.

### ✓ CloudFront POC distribution created

  Distribution ID: E34CQM3GCYZN5R
  Domain:          https://d1xzjd7z8lcp8a.cloudfront.net
  Origin:          sites.hudexchange.info.s3.amazonaws.com
  Origin path:     /trainings-poc
  Headers policy:  SecurityHeadersPolicy (preset)

  App confirmed loading at:
    https://d1xzjd7z8lcp8a.cloudfront.net/trainings/service-coordinators-in-multifamily-housing-online-learning-tool/index.html

### ✓ embed---sc-training switched from staticDir to embedUrl mode

  embed.config.json updated:
    was: { "staticDir": "smart-embeds/sc-training" }
    now: { "embedUrl": "https://d1xzjd7z8lcp8a.cloudfront.net/trainings/service-coordinators-in-multifamily-housing-online-learning-tool/index.html" }

  Module regenerated + reinstalled. App confirmed loading inside Drupal.
  Drupal repo no longer needs web/embeds/sc-training/ — static files not committed.
  This resolves the Pantheon repo bloat concern entirely for this approach.

### ✗ hudx-resize.js not yet injected into POC bucket

  The iframe loads but sits at fallback height (800px) — verticalspacer loop present.
  POC HTML files in S3 do not yet have the snippet injected.

  Plan:
    1. Write inject + S3 sync script
    2. Run against 3pd-ide/smart-embeds/sc-training/ locally (inject into source)
    3. Sync injected files to s3://sites.hudexchange.info/trainings-poc/ via AWS CLI

### WHERE WE LEFT OFF (end of 2026-03-30 session 3)

Completed this session:
  - CloudFront POC distribution live and serving the sc-training app
  - embed---sc-training switched to embedUrl mode (no files in Drupal repo)
  - Confirmed app loads inside Drupal via CloudFront

Pending:
  - Write inject + S3 sync script, run against POC bucket
  - Verify resize works after injection (verticalspacer fix + postMessage)
  - Fix lando drush path breakage in create-module.js tryExec (affects all starters)
  - Deploy react---3pd-embed-request to Pantheon
  - Deploy react---3pd-depot to Pantheon
  - Gate /hudx-test/* routes before production deployment
  - Other starters (react, astro, astro-forms) still need their own git repos


## SESSION: 2026-03-31 — hudx-resize.js injection + CloudFront debugging

### ✓ inject-and-sync-s3.js — script created

  3pd-ide/scripts/inject-and-sync-s3.js
  Injects hudx-resize.js into all .html files in a local source directory
  then syncs to S3 via AWS CLI. Usage:
    node inject-and-sync-s3.js <localDir> <s3Path>

  Fixed path bug: original script injected <script src="/hudx-resize.js"> (absolute,
  broke in CloudFront subdirectory). Corrected to <script src="hudx-resize.js"> (relative).

  S3 sync requires AWS CLI credentials — user does not have these for the client account.
  Workaround: inject locally, upload HTML files manually via S3 console.
  33 HTML files injected and uploaded to:
    s3://sites.hudexchange.info/trainings-poc/trainings/service-coordinators-in-multifamily-housing-online-learning-tool/

### ✗ hudx-resize.js verticalspacer fix — still unresolved

  Multiple fix iterations attempted:
  1. display: none !important on .verticalspacer → removed from DOM, footer overlapped header
  2. height: 0 !important CSS → Muse JS dynamically re-sets inline style, CSS didn't win
  3. hideMusePageUnits() JS → hid [data-pintopage] ancestor containers — accidentally hid
     MAIN CONTENT (pu85460 contains the app's primary nav + content, also has data-pintopage
     children). App appeared as blue box only.
  4. element.style.setProperty('height', '0', 'important') → JS important inline style.
     Should reliably override Muse's JS. Not yet confirmed working.

  Current hudx-resize.js state:
    - Uses setProperty with 'important' flag on .verticalspacer elements
    - collapseMuseSpacer() called inside sendHeight() on every event
    - No hideMusePageUnits() — removed (too aggressive)

  Current blocker: iframe shows blue box (fallback height 800px, no postMessage received).
  Root cause unknown — need to verify:
    1. Is hudx-resize.js loading in the iframe? (check Network tab on Drupal page)
    2. Does window.hudxSendHeight exist in the CloudFront page console?
    3. Is window.self !== window.top evaluating correctly?

  Also discovered: create-module.js for smart embed is export default only — must be
  invoked via CLI (node ../../starter-scripts/cli/index.js embed module --install),
  NOT via node create-module.js directly (runs silently, does nothing).

### CloudFront distribution details (POC)

  Distribution ID: E34CQM3GCYZN5R
  Domain:          https://d1xzjd7z8lcp8a.cloudfront.net
  Origin:          sites.hudexchange.info.s3.amazonaws.com / trainings-poc
  Headers policy:  None (SecurityHeadersPolicy was setting X-Frame-Options: sameorigin
                   which blocked the iframe — removed)
  Invalidations run: 2 (to clear cached old hudx-resize.js versions)

### WHERE WE LEFT OFF (end of 2026-03-31 session)

Completed this session:
  - inject-and-sync-s3.js script written and fixed (relative path)
  - 33 HTML files injected and uploaded to S3 POC bucket
  - hudx-resize.js revised to use JS setProperty (bypasses Muse inline style override)
  - Removed hideMusePageUnits() after it accidentally hid main page content
  - CloudFront SecurityHeadersPolicy removed (was blocking iframes)
  - Module regenerated via CLI (not direct node call)

Pending — pick up here next session:
  1. Verify window.hudxSendHeight exists on CloudFront page (open direct URL, check console)
  2. If undefined → hudx-resize.js not loading → check Network tab for 403/404
  3. If defined → postMessage not reaching parent → check Drupal console for message events
  4. Fix lando drush path breakage in create-module.js tryExec (affects all starters)
  5. Deploy react---3pd-embed-request to Pantheon
  6. Deploy react---3pd-depot to Pantheon
  7. Gate /hudx-test/* routes before production deployment
  8. Other starters (react, astro, astro-forms) still need their own git repos


## SESSION: 2026-03-31 (continued) — hudx-resize.js fully working

### ✓ CloudFront caching workflow — understood and locked in

  Every S3 file upload requires a CloudFront invalidation to propagate.
  Without invalidation, CloudFront serves the previous cached version.
  Workflow for every hudx-resize.js update:
    1. Upload new file via S3 console (AWS CLI blocked by RequireMFAforUsers IAM policy)
    2. CloudFront console → Distributions → E34CQM3GCYZN5R → Invalidations → Create → /*
    3. Wait ~30s → hard refresh (Cmd+Shift+R) to bypass browser cache

  AWS CLI credentials: scott.weimer IAM user has RequireMFAforUsers policy which blocks
  all CLI actions without MFA session token. Use S3/CloudFront console for all operations
  on this client account. Access keys exist but are insufficient without MFA.

### ✓ hudx-resize.js — final working state (all issues resolved)

  Root causes diagnosed via console.log debugging:
    1. Height locked at 5736px on every page (doc.scrollHeight)
       Muse sets an explicit height on the <html> element once, never updates across navigations.
       documentElement.scrollHeight is pinned to the tallest page loaded in the session.
       Fix: use body.scrollHeight ONLY — not Math.max(body, doc).
       body.scrollHeight correctly reflects each page's true content height.

    2. Height growing additively on each navigation (resize cascade)
       iframe height change → resize event fires in child → Muse recalculates → our handler
       fires immediately — order non-deterministic, can measure inflated height.
       Fix: debounce ALL events (resize, hashchange, popstate) through a single shared timer.
       600ms allows the postMessage → parent resize → child resize cascade to fully settle.

    3. Muse navigation: not SPA routing.
       Muse multi-page exports load separate .html files. hashchange/popstate do not fire.
       Navigation is detected via DOMContentLoaded on each new page load.
       resize events (from parent adjusting iframe height) are the main ongoing trigger.

    4. Accordion opens not detected
       Accordion opens change body height but don't trigger window events.
       Fix: ResizeObserver on document.body catches all DOM-level size changes,
       including accordion opens, without relying on window events.

  Final hudx-resize.js architecture:
    - collapseMuseSpacer(): setProperty 'important' on .verticalspacer (unchanged, works)
    - sendHeight(): body.scrollHeight only (was Math.max(body, doc))
    - debouncedSendHeight(): single shared 600ms debounce timer
    - Events: DOMContentLoaded → sendHeight() immediately (no debounce)
              resize / hashchange / popstate → debouncedSendHeight()
              ResizeObserver(body) → debouncedSendHeight()
    - window.hudxSendHeight exposed for manual calls

  Result: sc-training app loads correctly in iframe, resizes per page, accordions
  expand the iframe when opened. No additive extra space.

### Pantheon readiness for embed---sc-training

  The module uses embedUrl mode → no static files in the Drupal repo.
  Only the Drupal module needs to be committed and pushed:
    web/modules/custom/hudx_embed___sc_training/

  web/embeds/sc-training/ (old staticDir files) should be removed from the repo
  before the Pantheon push to avoid bloat.

### WHERE WE LEFT OFF (end of 2026-03-31 session 2)

Completed:
  ✓ hudx-resize.js body.scrollHeight fix (was doc.scrollHeight — locked at 5736px)
  ✓ Shared debounce for all events (resize cascade prevention)
  ✓ ResizeObserver for accordion detection
  ✓ CloudFront upload + invalidation workflow confirmed
  ✓ sc-training fully working in Drupal sandbox (resize + accordions)

Pending:
  - Remove web/embeds/sc-training/ from repo (no longer needed — using embedUrl)
  - Push embed---sc-training module to Pantheon
  - Fix lando drush path breakage in create-module.js tryExec (affects all starters)
  - Deploy react---3pd-embed-request to Pantheon
  - Deploy react---3pd-depot to Pantheon
  - Gate /hudx-test/* routes before production deployment
  - Other starters (react, astro, astro-forms) still need their own git repos
