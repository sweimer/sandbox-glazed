# SANDBOX-glazed — Session Log
# =========================================================
# Running notes for AI assistant continuity between sessions.
# Update this at the end of each session before closing.
# =========================================================


## PROJECT OVERVIEW

A Drupal 11 Lando site that embeds micro-frontend apps (React, Astro) as
Drupal blocks via a custom CLI toolkit at 3pd-ide/starter-scripts/cli/.

CLI command: `3pd`
Key commands:
  3pd astro app <name>          Create new Astro app
  3pd astro module --install    Build + install as Drupal block module
  3pd react app <name>          Create new React app
  3pd react module --install    Build + install as Drupal block module


## ARCHITECTURE

Lando services (2 only — do NOT add more):
  appserver   → Drupal 11 at http://sandbox-glazed.lndo.site
  database    → MariaDB 10.6

IMPORTANT: The astro Lando service was REMOVED. It caused HSTS poisoning of
all *.lndo.site subdomains. Do NOT add it back. No framework gets a Lando service.

React apps: Express runs on the HOST machine via npm run dev. No Lando service.
Astro apps: No Express. No Lando service. Static build + Drupal JSON:API.


## STARTER KITS

3pd-ide/apps/0.starter-react/        → React + Vite + Express + PostgreSQL [NEEDS CLEANUP]
3pd-ide/apps/0.starter-astro/        → Astro SSG + Drupal JSON:API (read-only) ✓ DONE
3pd-ide/apps/0.starter-astro-forms/  → Astro SSG + Express + SQLite ✓ DONE

EACH STARTER HAS A PROMPT.txt — READ IT AT THE START OF EVERY SESSION.
The PROMPT.txt files are the single source of truth for how each starter works.
They document architecture decisions, known issues, and lessons learned.
Always read the relevant PROMPT.txt before touching any starter kit code.

  0.starter-astro/PROMPT.txt       — Astro read-only pipeline, HAProxy gotchas, JSON:API patterns
  0.starter-astro-forms/PROMPT.txt — Astro + Express + SQLite, forms pattern, prod deployment
  0.starter-react/PROMPT.txt       — React pipeline, known issues pending fix (cleanup next)

3PD REPO STRATEGY (decided 2026-03-27):
Each starter kit will live in its own standalone repo. The 3PD (third-party) dev
clones only their relevant starter repo — they never see /web or the Drupal codebase.
`3pd <framework> module` (no --install) packages the Drupal module subfolder.
The 3PD dev pushes a feature branch. The Drupal owner pulls it and runs --install.
See "3PD WORKFLOW" section below for full details.


## ACTIVE APPS

react---3pd-depot     → Drupal module: hudx_react___3pd_depot
react---3pd-poc       → Drupal module: hudx_react___3pd_poc
react---stracat       → Drupal module: hudx_react___stracat
astro---poc-01        → Drupal module: hudx_astro___poc_01 (static, working)
astro---poc-02        → Drupal module: hudx_astro___poc_02 ← ACTIVE WORK


## LANDO COMMANDS

lando crx   — Full cache reset. Does: rm files/php/ + TRUNCATE all cache tables
              + stop Apache + drush status (pre-warms container into DB) + restart Apache.
              Use after every lando rebuild AND after enabling/installing modules.

lando drush — Standard drush. DO NOT use for cache clearing. Use lando crx.


## CRITICAL: lando crx / cache reset procedure

After lando rebuild OR after enabling modules OR when drush hangs:
  1. lando crx        ← handles everything
  2. Open browser     ← site loads immediately

NEVER use `lando drush cr` — it hangs. See SANDBOX-glazed/.lando.yml for
the full crx command. Root cause: Apache workers deadlock compiling the Drupal
service container simultaneously when cache is empty. crx stops Apache first,
pre-warms via drush status, then restarts Apache.

If URLs don't resolve after lando restart:
  lando poweroff && lando start   (re-registers proxy routes)
  If still broken: lando rebuild -y, then immediately lando crx


## SESSION: 2026-03-26 — What we fixed / built

### Astro starter kit — fully rebuilt
- Removed: Express, Prisma, SQLite, start.sh, Lando service entirely
- New architecture: Astro SSG + client-side Drupal JSON:API fetch
- No separate backend. No Lando service. Data from Drupal's own JSON:API.
- starter-template now has: package.json (astro only), index.astro (JSON:API
  client fetch), .env.example (PUBLIC_DRUPAL_BASE_URL for dev)
- astro-app.js: removed Prisma setup, removed start.sh patching, skips
  node_modules during copy (fixes broken .bin symlinks)
- react-app.js: same node_modules skip fix applied
- create-module.js: changed `lando drush cr` → `lando crx`
- PROMPT.txt written for 0.starter-astro (full reference doc)

### JSON:API module — enabled
- Was not enabled on this Drupal site
- Could not enable via `lando drush en` because of missing `canvas` module code
  (config exists in DB but no .info.yml on disk)
- Workaround: enabled via `drush php:eval` using module_installer service
- Routes activated via `drupal_flush_all_caches()` via php:eval

### astro---poc-02 — created and deployed
- Created with `3pd astro app poc-02`
- Updated index.astro to use `basic_page_layout_builder` (this site has no
  `page` content type nodes — types are: drag_and_drop_page,
  basic_page_layout_builder)
- Deployed with `3pd astro module --install`
- Block renders in Drupal but shows "No pages found."


## SESSION: 2026-03-27 — What we fixed / built

### astro---poc-02 — WORKING. Shows live Drupal data.

Root causes found and fixed:

1. HAProxy strips square brackets from query strings.
   `?fields[node--basic_page_layout_builder]=title` → `?fields=` (empty)
   Fix: removed the sparse fieldset param entirely. Plain URL with no params works.
   Note: %5B/%5D encoding also gets stripped by HAProxy — do NOT use brackets
   in query strings for Lando-proxied fetches.

2. Browser cache served stale JS after rebuild.
   `?v=1.x` was hardcoded in libraries.yml — never changed between builds.
   Fix: 0.starter-astro/create-module.js now uses Date.now() as version.
   Every rebuild produces a new ?v=<timestamp> cache buster.

3. Old starter template default URL was node/page — this site has no `page` nodes.
   Fix: poc-02 index.astro updated to fetch basic_page_layout_builder.
   The starter template (0.starter-astro) still uses node/page as placeholder —
   this is correct since it's generic. Apps must update it for their content type.

Block at node/6 now displays:
  - SANDBOX - 3PD Depot
  - SANDBOX - Stracat
  - Sandbox - Layout Builder


## SESSION: 2026-03-27 — Strategy decisions (no code changes)

### 3PD repo strategy — decided
Each starter kit gets its own standalone git repo.
3PD devs clone only their framework's repo. No /web, no Drupal, no Lando required.
`3pd <framework> module` (without --install) packages the module subfolder only.
3PD dev pushes a feature branch. Drupal owner pulls + runs --install.
The --install flag is INTERNAL USE ONLY (copies to /web + enables in Drupal).

### Data persistence — decided
Apps that collect data (forms etc.) use Express + SQLite. NOT Drupal's DB.
Reasons:
  1. Portability: if client changes CMS, Express + SQLite file moves intact.
  2. Compliance: .gov/HUD site — data must stay within FedRAMP boundary.
     No external PaaS (Railway, Render etc.) for data storage on this project.
  3. Simplicity: SQLite is a file, zero infrastructure overhead vs PostgreSQL.

In production: Express backend co-deployed on the Drupal server, behind the
same reverse proxy / auth wall as Drupal. Module JS calls /api/app-name/ on
the same domain — already protected by the auth layer, no extra token needed.

SQLite file location:
  Dev:  ./server/db/app.sqlite  (relative, inside app folder)
  Prod: /var/app-data/<appname>/app.sqlite  (absolute, outside web root)

### Client context — HUD (.gov federal agency)
The Drupal site is behind a sign-in wall for a HUD federal agency.
Federal guidance applies (FedRAMP, FISMA, NIST 800-53, Section 508).
Apps ONLY exist as Drupal modules in production — never standalone.
All data infrastructure must stay within the compliance boundary.
No unauthorized cloud services. Data must be US-hosted on approved infra.

### Starter kit plan — agreed order
1. Finish 0.starter-astro (read-only SSG — nearly done)
2. Build 0.starter-astro-forms (Astro SSG + Express + SQLite)
3. Update 0.starter-react (apply all lessons: SQLite, version string, drush path, cors)
4. Further frameworks (Vanilla JS, Angular, Svelte) follow the same pattern

### Two Astro starter variants
Read-only apps (display CMS content only):
  0.starter-astro → Astro SSG, no backend, no DB. Current pattern.
  Use when: app only needs to show Drupal content.

Apps with forms / data persistence:
  0.starter-astro-forms → Astro SSG + Express + SQLite.
  Express listens on 127.0.0.1 only (not 0.0.0.0 — not a public service).
  Use when: app needs to collect or store data.

### Mandatory checklist for ALL module generators (create-module.js)
See 0.starter-astro/PROMPT.txt for the full list. Key items:
  - version in libraries.yml = Date.now() (never static 1.x)
  - No square brackets in fetch URLs (HAProxy strips them)
  - lando crx for cache clear (never lando drush cr)
  - node_modules skipped during template copy
  - Express listens on 127.0.0.1 only in production builds
  - DB_PATH configurable via env var
  - Use .env.production for production build values (Vite/Astro reads this
    automatically during `astro build`). Set both PUBLIC_DRUPAL_BASE_URL=
    and PUBLIC_API_BASE_URL= to blank. .env keeps dev values (full URLs).
    This pattern is now in all starter templates — no manual blanking needed. (not hardcoded)


## SESSION: 2026-03-27 — Context compacted, picking up canvas fix

Canvas module remnants were blocking `lando drush en`. Investigation complete.
Canvas fix was IN PROGRESS when context ran out. See "WHERE WE LEFT OFF" below.

Root cause confirmed:
- canvas/canvas_stark appear in active Drupal config (core.extension) in the DB
- canvas appears in system.schema key_value table in the DB
- canvas-related sync config YAML files may exist in sites/default/files/config_*/sync/

Fix needed:
  1. Remove canvas from DB: delete from key_value where collection='system.schema'
     and name IN ('canvas', 'canvas_stark'); also update core.extension config.
  2. Remove canvas from sync config YAMLs if present.
  3. Run lando crx after.
  4. Then re-test: 3pd astro-forms module --install (verify lando drush en succeeds)
  5. Verify test route: http://sandbox-glazed.lndo.site/hudx-test/<appname>

After canvas fix, NEXT: React starter cleanup (see "NEXT" section below).


## WHERE WE LEFT OFF (pick up here next session)

### ✓ 0.starter-astro — DONE (2026-03-27)
  - Clean read-only Astro SSG starter, no backend, no DB
  - HAProxy bracket issue documented + fixed in template
  - version: Date.now() in create-module.js
  - node_modules removed from starter-template, .gitignore added
  - .env.example uses generic placeholder URL
  - index.astro has clear YOUR_CONTENT_TYPE placeholder with dev guidance
  - PROMPT.txt complete including mandatory checklist

### ✓ 0.starter-astro-forms — DONE + FULLY TESTED (2026-03-27)
  - Astro SSG + Express + SQLite (better-sqlite3 ^11.0.0)
  - server/server.js: 127.0.0.1 bind, explicit CORS, GET+POST /api/submissions
  - server/db/database.js: singleton, WAL mode, auto-schema on startup
  - server/db/schema.sql: submissions table
  - CLI: 3pd astro-forms app <name> / 3pd astro-forms module [--install]
  - Tested end-to-end: form submits, SQLite persists, list renders
  - concurrently added — npm run dev starts both Express + Astro in one command
    (dev:server and dev:astro available individually for debugging)
  - 3pd help rewritten: workflow steps at top, starter kit descriptions, 3PD vs --install explanation
  - PROMPT.txt complete
  - NOTE: better-sqlite3 must be ^11.0.0+ for Node 23 (v9.x fails to compile)
  - .env.production added to starter-template — Astro/Vite uses this automatically
    during `astro build`, baking in blank PUBLIC_* vars for same-origin production use.
    .env keeps full dev URLs. No manual blanking needed before --install.
  - CORS configured in web/sites/default/services.yml to allow localhost:4321
    so the Astro dev server can fetch Drupal JSON:API cross-origin during development.
  - Submissions 404 on Drupal test route is EXPECTED on Lando — Express runs on the
    host Mac, not inside the container. Reverse proxy gap closes in real deployment.
    Express must be running (via PM2 in production) for /api/ to connect.

### REPO STRUCTURE — decided (2026-03-27)

Standalone starter repos live as siblings at the Drupal root level:

  SANDBOX-glazed/
    web/
    3pd-astro/           ← standalone repo (3PD devs clone this)
    3pd-astro-forms/     ← standalone repo
    3pd-react/           ← standalone repo
    3pd-ide/             ← internal workspace (active apps, --install tooling)

Each starter repo is fully self-contained: starter-template/, cli/, create-module.js,
package.json (with 3pd bin), PROMPT.txt. No shared CLI across repos.

3pd-ide stays as the internal monorepo for running --install, managing active apps,
and Drupal integration. 3PD devs never see it.

Option A (all inside 3pd-ide/) was rejected — nested git repos create friction
and 3PD devs would see sibling starters they don't need.

CLI refactor needed: each repo gets its own trimmed cli/ (single framework only).
Do this when extracting each starter into its own repo.


### ✓ 0.starter-react — DONE (2026-03-27)
  - PostgreSQL → SQLite (better-sqlite3 ^11.0.0)
  - version: Date.now() in libraries.yml
  - Fixed drush commands: lando drush en + php:eval fallback + lando crx
  - Fixed Express cors() to explicit options, bind to 127.0.0.1
  - Added test route (routing.yml + HudxTestController.php) to create-module.js
  - Added .env.production with blank VITE_API_BASE_URL
  - Consolidated to single root .env (removed server/.env.example)
  - Removed PostgreSQL setup from react-app.js (no createdb/psql)
  - Rewrote PROMPT.txt to match Astro starter standard
  - NOTE: React uses VITE_ prefix (not PUBLIC_) for client-exposed env vars


## CONTENT TYPES ON THIS SITE

  drag_and_drop_page          (nid 4: "Sandbox - DXPR")
  basic_page_layout_builder   (nid 6: "Sandbox - Layout Builder",
                               nid 10: "SANDBOX - 3PD Depot",
                               nid 11: "SANDBOX - Stracat")

There is NO `page` content type with nodes. The starter template's
`node/page` default fetch URL needs to be updated to `basic_page_layout_builder`
for this specific site.


## 3PD WORKFLOW (third-party developer process)

3PD dev (no access to /web Drupal codebase):
  1. Clone their framework's starter repo (e.g. 0.starter-astro)
  2. Run: 3pd astro app <feature-name>
  3. Build the feature in their app folder
  4. Run: 3pd astro module   (no --install — packages module subfolder only)
  5. Push feature branch containing the module subfolder
  6. Open PR to main repo

Drupal owner (this repo):
  1. Pull the feature branch
  2. Run: 3pd astro module --install   (copies to /web/modules/custom + enables)
  3. Run: lando crx
  4. Place block in Layout Builder

The module subfolder is the deliverable. The app src/ is the development environment.


## REVISIT IN CI/CD DISCUSSION

### Test route in generated modules (/hudx-test/<appname>)
Every generated Drupal module includes a HudxTestController + routing.yml
that creates a /hudx-test/<appname> page for development testing.
This route is gated by 'access content' permission only.

Before production deployment this needs one of:
  A) Remove routing.yml + src/Controller/ from the module before deploy
  B) Gate route behind a stricter permission (e.g. 'administer site configuration')
  C) Disable via environment-aware config (if dev/prod environments are split)

The right answer depends on the CI/CD pipeline design.
Flag this when starting the CI/CD discussion.


## KNOWN PENDING ISSUES

- `canvas` module: FIXED (2026-03-27). Removed from DB active config + system.schema
  key_value + all canvas.* sync config YAMLs. `lando drush en` now works cleanly.
- React create-module.js still has old drush path (cd /app/web && drush).
  Fix when next touching React starter. See 0.starter-react/PROMPT.txt.
- React server.js: bare cors() + missing 0.0.0.0 binding.
  Fix when next touching React starter.
- Docker Desktop update recommended (shown in lando output warnings).
- OPcache CLI not yet enabled (was causing startup issues — skip for now).


## README.md

Created README.md at the repo root (2026-03-28) for lower-level devs setting up locally.
Covers: prerequisites, first-time setup, settings.php DB block, lando crx explanation,
troubleshooting, project structure, 3pd CLI usage, content types, Pantheon info.

KEEP THIS UPDATED. Any time the setup process changes — new prereqs, new lando commands,
new content types, Pantheon URL changes, CLI command changes — update README.md to match.


## SESSION: 2026-03-28 — Pantheon setup

### Goal
Stand up a shared Pantheon Dev environment so 3PD devs can point their apps at a
real live Drupal JSON:API endpoint (instead of running Lando locally).

### What was done

1. Created Pantheon site "3PD IDE" (machine name: 3-pd-ide) using Drupal (Composer Managed) upstream.
   Site ID: 5921a711-db26-437b-93a3-6c5aa093be3d
   Dev URL: https://dev-3-pd-ide.pantheonsite.io

2. Created .github/workflows/deploy-pantheon.yml — pushes main → Pantheon master on every push to main.
   Uses two GitHub repo secrets:
     PANTHEON_SSH_KEY  — RSA private key (ed25519 not supported by Pantheon)
     PANTHEON_GIT_URL  — ssh://codeserver.dev.5921a711-db26-437b-93a3-6c5aa093be3d@codeserver.dev.5921a711-db26-437b-93a3-6c5aa093be3d.drush.in:2222/~/repository.git

3. Added pantheon.yml (NOT pantheon.upstream.yml — that's reserved for upstream):
     api_version: 1
     web_docroot: true
     build_step: true
     php_version: 8.3
     database:
       version: 10.6

4. Fixed settings.php — wrapped lando DB credentials in isset($_ENV['LANDO_APP_NAME'])
   check so they only apply locally. Added Pantheon settings include at bottom:
     if (file_exists('/var/www/html/sites/default/settings.pantheon.php')) {
       require '/var/www/html/sites/default/settings.pantheon.php';
     }

5. Exported database as db/3pddb5.sql.gz (stripped MariaDB 11.x header line that
   breaks MariaDB 10.6). Successfully imported to Pantheon via:
     terminus import:database 3-pd-ide.dev https://raw.githubusercontent.com/sweimer/sandbox-glazed/main/db/3pddb5.sql.gz

6. SSH keys for Pantheon (RSA only — ed25519 not supported):
   - pantheon_deploy  (RSA) — for GitHub Actions
   - pantheon_local   (RSA) — for local terminus/drush commands
   Both added via `terminus ssh-key:add`.
   Also added id_samhsa_rsa_m1.pub as fallback local key.
   Run `ssh-add ~/.ssh/pantheon_local` before using terminus remote:drush.

### WHERE WE LEFT OFF

Pantheon build is failing at 95% during "Sync code on dev" workflow.
Root cause: drupal/webform is pinned to `6.3.x-dev` in composer.json — this requires
cloning a git repo instead of downloading a release, likely timing out on Pantheon's
sandbox tier.

Fix in progress (NOT yet committed/tested):
  - Changed composer.json: "drupal/webform": "6.3.x-dev" → "^6.2"
  - Next step: run `lando composer update drupal/webform --with-dependencies`
    to regenerate composer.lock, then commit both files and push.

After that, watch Pantheon dashboard → Dev → Workflows for successful build.
Then verify: https://dev-3-pd-ide.pantheonsite.io should load Drupal.

### AFTER PANTHEON IS UP

- Update .env in all 3pd-ide/apps/* to use Pantheon URL instead of lando URL
  for PUBLIC_DRUPAL_BASE_URL (so apps can be tested without Lando running)
- Update astro-app.js and astro-forms-app.js auto-detect fallback to suggest
  Pantheon URL if lando is not running
- Standalone repo extraction: create 3pd-astro/, 3pd-astro-forms/, 3pd-react/
  as independent repos alongside 3pd-ide/ at the SANDBOX-glazed root
- CI/CD discussion: test route gating, --install → git commit+push for Pantheon
