---
export interface Props {
  title: string;
}

const { title } = Astro.props;

// Drupal theme styles are loaded during local dev only.
// import.meta.env.DEV is false during `astro build`, so these <link> tags
// are never included in the compiled module output.
// Generate public/drupal-dev-styles.css by running: 3pd styles sync
const isDev = import.meta.env.DEV;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    {isDev && <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.min.css" />}
    {isDev && <link rel="stylesheet" href="/drupal-dev-styles.css" />}
  </head>
  <body>
    <slot />
    {isDev && <script is:inline src="/drupal-dev-scripts.js"></script>}
  </body>
</html>

<style is:global>
  * {
    box-sizing: border-box;
  }

  body {
    font-family: system-ui, sans-serif;
    margin: 0;
    padding: 0;
  }
</style>
