# Production build values — used automatically by Astro/Vite during `astro build`.
# Both PUBLIC_* vars are blank so the built JS fetches same-origin inside Drupal.
# The reverse proxy routes /api/ to Express on the same domain.
PUBLIC_DRUPAL_BASE_URL=
PUBLIC_API_BASE_URL=
