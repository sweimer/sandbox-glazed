# Production build values — used automatically by Vite during `vite build`.
# VITE_API_BASE_URL is blank so the built JS posts to /api/ same-origin inside Drupal.
# The reverse proxy routes /api/ to Express on the same server.
VITE_API_BASE_URL=
