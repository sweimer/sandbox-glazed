<?php

/**
 * Implements hook_schema().
 */
function hudx_3pd_ai_coder_schema() {
  $schema['hudx_3pd_ai_coder_history'] = [
    'description' => 'AI markup generation history for the 3PD AI Helper.',
    'fields' => [
        'id' => [
          'type' => 'serial',
          'unsigned' => TRUE,
          'not null' => TRUE,
        ],
        'title' => [
          'type' => 'text',
          'size' => 'big',
          'not null' => FALSE,
        ],
        'prompt' => [
          'type' => 'text',
          'size' => 'big',
          'not null' => FALSE,
        ],
        'markup' => [
          'type' => 'text',
          'size' => 'big',
          'not null' => FALSE,
        ],
        'node_url' => [
          'type' => 'text',
          'size' => 'big',
          'not null' => FALSE,
        ],
        'created_at' => [
          'type' => 'text',
          'size' => 'big',
          'not null' => FALSE,
        ],
    ],
    'primary key' => ['id'],
  ];
  return $schema;
}

/**
 * Implements hook_install().
 */
function hudx_3pd_ai_coder_install() {
  _hudx_3pd_ai_coder_import_seed();
}

function _hudx_3pd_ai_coder_import_seed() {
  $seed_path = \Drupal::service('extension.list.module')->getPath('hudx_3pd_ai_coder') . '/data/seed.json';
  if (!file_exists($seed_path)) return;
  $seed = json_decode(file_get_contents($seed_path), TRUE);
  if (empty($seed['rows'])) return;
  $db = \Drupal::database();
  $db->truncate('hudx_3pd_ai_coder_history')->execute();
  foreach ($seed['rows'] as $row) {
    unset($row['id']);
    $db->insert('hudx_3pd_ai_coder_history')->fields($row)->execute();
  }
}
