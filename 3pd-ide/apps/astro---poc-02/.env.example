# Base URL of your Drupal site — used only for standalone dev server.
# Leave blank (or omit) when the app is embedded inside Drupal (same-origin fetch).
PUBLIC_DRUPAL_BASE_URL=http://sandbox-glazed.lndo.site
